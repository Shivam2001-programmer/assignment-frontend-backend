# Leave unset locally: requests go to /api and the Vite dev server proxies them to :4000.
# For a deployment on a different origin, set the backend's public URL:
# VITE_API_URL=https://lead-intake-api.onrender.com
